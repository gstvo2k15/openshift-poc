apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring-acc
resources:
  - ../../base
patches:
  - target:
      kind: Namespace
      name: monitoring-template
    patch: |-
      - op: replace
        path: /metadata/name
        value: monitoring-acc
commonLabels:
  environment: acc
